# Agents

- Do not run `git checkout`, `git reset`, or any other Git command that modifies existing files. All work must be done by editing files directly and leaving other contributors' changes intact.
