// Package router hosts the next-generation router runtime used to coordinate
// URL state, navigation history, and rendering without relying on fragment
// placeholders. The implementation is currently experimental and will coexist
// alongside the legacy router until feature parity is reached.
package router
